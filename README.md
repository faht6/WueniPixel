# WueniPixel - Boutique Tecnológica

Este proyecto es una tienda de comercio electrónico desarrollada con React + Vite.

## 🚀 Despliegue Automático (Recomendado)

Para que tu página se actualice automáticamente, debes conectarla a GitHub y Vercel/Netlify.

### Paso 1: Subir a GitHub
1.  Crea un nuevo repositorio en [GitHub.com](https://github.com/new) (llámalo `wuenipixel`).
2.  Copia los comandos que GitHub te muestra para "push an existing repository". Deberían ser similares a:
    ```bash
    git remote add origin https://github.com/TU_USUARIO/wuenipixel.git
    git branch -M main
    git push -u origin main
    ```
    *(Ejecuta estos comandos en tu terminal)*.

### Paso 2: Conectar con Vercel
1.  Ve a [Vercel.com](https://vercel.com) y regístrate con GitHub.
2.  Haz clic en **"Add New Project"**.
3.  Selecciona el repositorio `wuenipixel` que acabas de subir.
4.  Haz clic en **"Deploy"**.

¡Listo! Vercel te dará una URL (ej. `wuenipixel.vercel.app`).
**Cada vez que guardes cambios y hagas `git push`, tu página se actualizará sola.**

## 🛠 Comandos Útiles

- `npm run dev`: Inicia el servidor de desarrollo.
- `npm run build`: Genera la versión de producción en `dist/`.
- `git add .` + `git commit -m "Mensaje"`: Guarda tus cambios localmente.
- `git push`: Sube tus cambios a la nube.
